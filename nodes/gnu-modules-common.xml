<?xml version="1.0" standalone="no"?>

<kickstart>

<description>
GNU
http://www.allinea.com
</description>

<copyright>
Copyright (c) 2000 - 2011 The Regents of the University of California.
All rights reserved. Rocks(r) v5.1 www.rocksclusters.org
</copyright>

<changelog>
</changelog>

<post>

# Remove /etc/profile.d files in favor of loadable modules when available
if test -f /etc/profile.d/gnu.sh; then

  /bin/rm -f /etc/profile.d/gnu.*

  if test `grep -c /opt/modulefiles/applications /etc/profile.d/modules.csh` = 0; then
    echo 'setenv MODULEPATH /opt/modulefiles/applications:$MODULEPATH' >> \
         /etc/profile.d/modules.csh
    echo 'if ( -s ${HOME}/.modules ) then' >> /etc/profile.d/modules.csh
    echo 'source ${HOME}/.modules' >> /etc/profile.d/modules.csh
    echo 'endif' >> /etc/profile.d/modules.csh

  fi
  if test `grep -c /opt/modulefiles/applications /etc/profile.d/modules.sh` = 0; then
    echo 'export MODULEPATH=/opt/modulefiles/applications:$MODULEPATH' >> \
         /etc/profile.d/modules.sh
    echo 'if test -f ${HOME}/.modules; then' >> /etc/profile.d/modules.sh
    echo '. ${HOME}/.modules' >> /etc/profile.d/modules.sh
    echo 'fi' >> /etc/profile.d/modules.sh
  fi

  /bin/mkdir -m 0755 -p /opt/modulefiles/applications/gnu

<file name="/opt/modulefiles/applications/gnu/4.6.1" perms="0644">
#%Module1.0
prepend-path PATH /opt/gnu/gcc/4.6.1/bin
prepend PATH /opt/gnu/gmp/4.3.2/bin
prepend PATH /opt/gnu/mpfr/3.0.1/bin 
prepend PATH /opt/gnu/mpc/0.9/bin
setenv LD_LIBRARY_PATH /opt/gnu/gcc/4.6.1/lib64
setenv LD_LIBRARY_PATH /opt/gnu/gmp/4.3.2/lib
setenv LD_LIBRARY_PATH /opt/gnu/mpfr/3.0.1/lib
setenv LD_LIBRARY_PATH /opt/gnu/mpc/0.9/lib
</file>

<file name="/opt/modulefiles/applications/gnu/.version.4.6.1">
#%Module1.0
set ModulesVersion "4.6.1"
</file>

  /bin/ln -s /opt/modulefiles/applications/gnu/.version.4.6.1 \
             /opt/modulefiles/applications/gnu/.version

fi

</post>

</kickstart>
